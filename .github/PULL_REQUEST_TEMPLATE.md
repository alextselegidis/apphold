## Summary

<!-- What does this pull request change, and why? -->

## Related issue

<!-- e.g. Closes #123 -->

## Checklist

- [ ] The change is focused on a single topic.
- [ ] `docker compose exec php-fpm php artisan test` passes.
- [ ] Pint and Prettier were run on the touched files.
- [ ] New user facing strings go through `__()` and are added to `lang/en.json`.
- [ ] `openapi.yml` is in sync if API routes changed.
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]` for user visible changes.

## Screenshots

<!-- For UI changes, before and after. -->
